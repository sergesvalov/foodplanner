# 📅 FoodPlanner

**FoodPlanner** — это full-stack веб-приложение для планирования питания семьи, учета стоимости продуктов, подсчета калорий и БЖУ (белков, жиров, углеводов), а также генерации умного списка покупок с интеграцией Telegram.

---

## 🏗 Структура проекта

Проект разделен на две основные части: **Backend** (API) и **Frontend** (UI).

### 📂 Backend (`/backend`)
Технологический стек: **Python, FastAPI, SQLAlchemy (SQLite), Alembic**.

- **`main.py`**: Точка входа в backend. Инициализация FastAPI приложения, настройка CORS, подключение роутеров.
- **`database.py`**: Конфигурация базы данных и создание сессий.
- **`models.py`**: ORM модели базы данных (SQLAlchemy).
    - `User`: Пользователи (члены семьи), их настройки и калораж.
    - `Product`: Ингредиенты с данными о нутриентах и цене.
    - `Recipe`: Рецепты, состоящие из продуктов (`RecipeIngredient`).
    - `PlanItem`: Элементы планирования (день, прием пищи, пользователь, рецепт, порции).
- **`schemas.py`**: Pydantic схемы для валидации входных и выходных данных API.
- **`routers/`**: Модули API, сгруппированные по функционалу:
    - `admin.py`: Управление пользователями и глобальными настройками.
    - `plan.py`: Логика планирования, авто-распределения и управления приемами пищи.
    - `products.py`: CRUD операции с продуктами.
    - `recipes.py`: CRUD операции с рецептами.
    - `shopping_list.py`: Генерация списка покупок и интеграция с Telegram.
- **`services/`**: Вспомогательная бизнес-логика.
- **`alembic/`**: Миграции базы данных.

### 📂 Frontend (`/frontend`)
Технологический стек: **React (Vite), Tailwind CSS**.

- **`src/pages/`**: Основные страницы приложения.
    - **`PlanningPage.jsx`**: (Неделя) Главная страница планирования. Drag & Drop интерфейс, авто-распределение.
    - **`TodayPage.jsx`**: (Сегодня) Меню на текущий день с режимом готовки.
    - **`StatisticsPage.jsx`**: (Статистика) Аналитика по калориям, БЖУ и стоимости за неделю.
    - **`ShoppingListPage.jsx`**: (Список покупок) Генерация списка продуктов на выбранный период.
    - **`RecipesPage.jsx`**: (Рецепты) Каталог рецептов, создание и редактирование (`RecipeBuilder`).
    - **`ProductsPage.jsx`**: (Продукты) База ингредиентов.
    - **`AdminPage.jsx`**: (Семья) Управление профилями членов семьи.
    - **`AboutPage.jsx`**: (О проекте) Информация о приложении.
- **`src/components/`**: Переиспользуемые UI компоненты.
    - `planning/`: Компоненты для страницы планирования (`WeeklyBoard`, `RecipeCard`, `RecipeList`).
    - `stats/`: Компоненты статистики (`NutritionCard`, `DailyStatsTable`).
    - `ui/`: Общие UI элементы (кнопки, модальные окна, инпуты).
- **`src/hooks/`**: Кастомные хуки для управления состоянием (`usePlanning`, `useStatistics`).
- **`src/api/`**: Функции для взаимодействия с Backend API.

---

## 🚀 Основные возможности

### 1. Умное Планирование (Planning Page)
- **Drag & Drop**: Перетаскивайте рецепты из списка (слева) в дни недели (справа).
- **Авто-распределение (Auto-Distribute)**: Кнопка "Авто" заполняет пустые слоты на неделю, учитывая предпочтения пользователей и историю (чтобы не есть одно и то же).
- **Персонализация**: Назначайте блюда конкретным членам семьи или всем сразу.
- **Управление порциями**: Регулируйте количество порций для каждого приема пищи.

### 2. Меню на Сегодня (Today Page)
- Просмотр плана на текущий день.
- Режим "Готовка": Отмечайте выполненные шаги рецепта и собранные ингредиенты.
- Кнопка "Дожрать": Быстрое добавление остатков вчерашней еды или случайного перекуса.

### 3. Статистика (Statistics Page)
- Наглядные графики потребления калорий, белков, жиров и углеводов.
- Сравнение с дневными нормами каждого члена семьи.
- Учет стоимости питания за неделю.

### 4. Список Покупок (Shopping List Page)
- Выберите период (например, следующая неделя).
- Приложение автоматически сгенерирует список необходимых продуктов, суммируя вес ингредиентов из всех запланированных рецептов.
- **Telegram Интеграция**: Отправка готового списка покупок (с ценами и итогами) в Telegram бот.

### 5. База Знаний (Recipes & Products)
- **Продукты**: Хранят КБЖУ и цену.
- **Рецепты**: Автоматически считают свою калорийность и стоимость на основе ингредиентов.
- **Конструктор Рецептов**: Удобное создание блюд с динамическим пересчетом нутриентов.

---

## 🛠 Установка и Запуск

### Предварительные требования
- Python 3.9+
- Node.js 18+
- Docker (опционально)

### Запуск Backend
1. Перейдите в папку `backend`:
   ```bash
   cd backend
   ```
2. Создайте виртуальное окружение и активируйте его:
   ```bash
   python -m venv venv
   # Windows:
   venv\Scripts\activate
   # Linux/Mac:
   source venv/bin/activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Примените миграции базы данных (если требуется):
   ```bash
   alembic upgrade head
   ```
5. Запустите сервер:
   ```bash
   uvicorn main:app --reload
   ```
   *API будет доступно по адресу: `http://localhost:8000`*
   *Документация API (Swagger): `http://localhost:8000/docs`*

### Запуск Frontend
1. Перейдите в папку `frontend`:
   ```bash
   cd frontend
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите сервер разработки:
   ```bash
   npm run dev
   ```
   *Приложение будет доступно по адресу, указанному в консоли (обычно `http://localhost:5173`)*

---

## 📝 Руководство пользователя

1. **Настройка Семьи**: Зайдите в раздел "Семья" (Admin) и создайте профили для всех, кого планируете кормить. Укажите их нормы калорий и БЖУ.
2. **База Продуктов**: В разделе "Продукты" добавьте базовые ингредиенты, которые вы используете (яйца, молоко, курица и т.д.), указав их КБЖУ и цену.
3. **Создание Рецептов**: В разделе "Рецепты" создайте свои любимые блюда из добавленных продуктов.
4. **Планирование**: Перейдите на страницу "Неделя". Используйте авто-распределение или вручную перетащите рецепты в нужные дни.
5. **Покупки**: Перед походом в магазин зайдите в "Список покупок", выберите нужные даты и отправьте список себе в Telegram.

---

## ℹ️ Дополнительная информация

### Telegram Bot
Для работы интеграции с Telegram необходимо создать бота через `@BotFather` и получить токен. ID вашего чата можно узнать через `@userinfobot`. Эти данные указываются в настройках (AdminPage) или в переменных окружения `.env` (если настроено).

### Docker
В корне проекта есть `docker-compose.yml` для быстрого развертывания всего стека одной командой:
```bash
docker-compose up --build
```
