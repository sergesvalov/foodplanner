# 📅 FoodPlanner

**FoodPlanner** — это full-stack веб-приложение для планирования питания, учета стоимости продуктов, подсчета калорий и БЖУ (белков, жиров, углеводов), а также генерации умного списка покупок с интеграцией Telegram.

---

## 🚀 Новые возможности

### 🔮 Логика Авто-распределения (Auto-Distribution Logic)

Алгоритм `autoDistribute` в `usePlanning.js` выполняет автоматическое заполнение расписания на неделю, следуя строгим правилам персонализации и последовательности. Это позволяет нейросети или разработчику понять, *как* именно принимаются решения о распределении порций.

#### 1. Подготовка и Контекст
Перед очисткой текущей сетки расписания алгоритм анализирует **текущие намерения пользователя**:
*   **Идентификация потребителей**: Если блюдо уже добавлено в план, оно считается "закрепленным" за конкретными пользователями.
    *   *Пример*: Если "Омлет" стоит у "Мамы", авто-распределение будет предлагать "Омлет" **только** "Маме".
    *   *Исключение*: Блюда, которых нет в текущем плане, доступны для распределения всем членам семьи.
*   **История Завтраков**: Собирается статистика частоты потребления завтраков для каждого пользователя из текущего плана.

#### 2. Сортировка и Фильтрация
Рецепты сортируются по рейтингу (от высокого к низкому), чтобы приоритет отдавался любимым блюдам.
*   **Исключение**: Рецепты категории `side` (гарниры) полностью исключены из авто-распределения.

#### 3. Распределение Завтраков (History with Fallback)
Завтраки распределяются **на основе истории** текущего плана с автоматическим резервным вариантом.
*   **Алгоритм**:
    1. Для каждого пользователя проверяется наличие истории завтраков в текущем плане.
    2. **Если история есть**: Берется список рецептов завтраков, которые пользователь ел, отсортированный по частоте. Дни недели заполняются **последовательно** (Пн -> Вс) с учетом количества из истории.
    3. **Если истории нет** (новый пользователь или очищенный план): Автоматически распределяются доступные рецепты завтраков последовательно по дням недели, начиная с самых высокорейтинговых.
    4. Пользователь получает только те блюда, для которых есть доступные порции.

#### 4. Распределение Обедов и Ужинов (Sequential Timeline)
Для категорий `main` (второе) и `soup` (суп) используется модель **непрерывной временной шкалы**.
*   **Шкала времени**: Создается последовательность слотов: `Пн Обед` -> `Пн Ужин` -> `Вт Обед` -> ... -> `Вс Ужин`.
*   **Алгоритм**:
    1. Итерируемся по списку доступных рецептов (Mains/Soups).
    2. Проверяем **ограничение потребителей**: Если рецепт "приватный" (см. п.1), кандидатами на получение являются только его "владельцы". Иначе — все члены семьи.
    3. Начинаем заполнение шкалы строго с **начала недели (Понедельник Обед)**.
    4. Двигаемся по шкале последовательно. В каждом слоте раздаем порции всем доступным кандидатам, которые еще не ели в этот прием пищи.
    5. Если порции рецепта заканчиваются, переходим к следующему рецепту. Если слот заполнен, переходим к следующему слоту в шкале.
    6. **Супы**: Распределяются только в слоты `Обед`.

Этот подход гарантирует:
*   **Отсутствие хаоса**: Блюда идут подряд (доели одно -> начали другое).
*   **Персонализацию**: Учитываются индивидуальные диеты/предпочтения, заданные вручную в текущем плане.
*   **Предсказуемость**: Планирование всегда начинается с начала недели.

### 📊 Учет нутриентов (БЖУ)
* **Продукты**: Хранят информацию о белках, жирах и углеводах (на 100г).
* **Рецепты**: Автоматический расчет БЖУ блюда на основе ингредиентов.
* **Гибкие единицы**: Поддержка "1 стакана" (150г/мл) для удобного ввода.
* **Логика расчета**: [Подробная инструкция по расчету КБЖУ](frontend/src/components/README.md).
* **Отображение**: Цветные бейджи с граммами Б/Ж/У в карточке рецепта и в сетке плана.

### 📅 Навигация и Управление
* **Drag & Drop**: Перетаскивание блюд между днями и приемами пищи для быстрой корректировки плана.
* **Календарь**: Выбор любой даты для просмотра истории или планирования на далекое будущее.
* **Сортировка**: Ингредиенты в списке выбора отсортированы по алфавиту для удобного поиска.
* **Недельный просмотр**: Удобное переключение между неделями.

---

## 🏗 Структура проекта

Проект разделен на Backend (API) и Frontend (UI). Используется модульная архитектура.

### 📂 Backend (`/backend`)
Написан на **Python (FastAPI)** + **SQLAlchemy** (SQLite).

* **`main.py`**: Точка входа. Инициализация `FastAPI`, подключение CORS.
* **`routers/`**:
    * `products.py`: Управление продуктами (CRUD + Import/Export с нутриентами).
    * `recipes.py`: Рецепты и расчет их стоимости/калорийности/БЖУ.
    * `plan.py`: Умное планирование, управление записями, авто-заполнение.
    * `shopping_list.py`: Генерация списка с ценами и отправка в Telegram.
    * `admin.py`: Настройки семьи и интеграций.

### 📂 Frontend (`/frontend`)
Написан на **React (Vite)** + **Tailwind CSS**.

* **`WeeklyGrid.jsx`**: Основная сетка. Drag & Drop, навигация по датам, отображение БЖУ.
* **`TodayPage.jsx`**: "Меню на сегодня" с детальным рецептом и итоговой сводкой КБЖУ.
* **`RecipeBuilder.jsx`**: Конструктор рецептов с живым пересчетом метрик.
* **`ShoppingListPage.jsx`**: Список покупок с разделением по категориям и ценами.

---

## 🧠 Логика данных

### 1. Продукты (Products)
Базовая единица.
* **`calories` / `proteins` / `fats` / `carbs`**: Пищевая ценность (на 100г).
* **`price`**: Цена за упаковку.
* **`amount`**: Вес упаковки.

### 2. Рецепты (Recipes)
Состоит из ингредиентов.
* **Расчет цены**: Сумма стоимости использованных частей продуктов.
* **Расчет БЖУ**: Сумма нутриентов ингредиентов (с учетом веса).

### 3. План на неделю (WeeklyPlan)
* **Персонализация**: Каждая запись может быть привязана к конкретному члену семьи.
* **Шаблонизация**: Возможность сохранить текущую неделю как шаблон.

### 4. Список покупок (Shopping List)
* Агрегирует ингредиенты из выбранного диапазона дат.
* **Цены**: Показывает примерную стоимость каждой позиции и итоговый чек.
* **Telegram**: Отправляет в мессенджер красивый список с ценами.

---

## 🎮 Интерактивность и Управление

### 1. Совместное Планирование
* Все изменения в плане (добавление, перенос, удаление) мгновенно сохраняются в **базе данных**.
* Состояние синхронизируется между всеми открытыми вкладками/устройствами.

### 2. Drag & Drop и Назначение
* **Боковая панель**: Рецепты можно перетаскивать из левой панели прямо в дни недели.
* **Умное назначение**:
    * Если в фильтре выбрано **"Все"**: При перетаскивании появится всплывающее окно с вопросом "Для кого?". Можно выбрать "Для всех" (добавит каждому члену семьи) или конкретного человека.
    * Если выбран **конкретный пользователь**: Блюдо автоматически назначается ему.
* **Смена владельца**: Клик по аватарке на карточке блюда переключает его на следующего члена семьи.

### 3. Кнопка "Дожрать" (Smart Snack)
Умная кнопка для быстрого заполнения текущего слота:
* **Логика времени**:
    * 00:00 - 11:00: **Завтрак**
    * 11:00 - 14:00: **Обед**
    * 14:00 - 18:00: **Полдник**
    * 18:00 - 00:00: **Ужин**
* **Приоритет доедания**: Для Обеда и Ужина система сначала ищет **недавно приготовленные** Супы и Вторые блюда (Leftovers), чтобы предложить доесть их. Если истории нет — предлагает случайное блюдо/перекус.
* **Мульти-слоты**: Можно нажимать несколько раз, чтобы добавить несколько перекусов в одно время.
* **Контроль калорий**: Если добавление блюда превысит дневную норму пользователя, система выдаст **предупреждение**, но позволит добавить блюдо.

---

## 🤖 Интеграции

### Telegram Bot
* Отправляет список покупок прямо в чат.
* Сообщение содержит:
    * Список продуктов (с весом и кол-вом упаковок).
    * Примерную цену за каждую позицию.
    * **Итоговую стоимость** всей корзины.

---

## 🛠 Технический стек
* **Language**: Python 3.9+ / JavaScript (ES6+)
* **Frameworks**: FastAPI / React 18
* **DB**: SQLite (via SQLAlchemy ORM)
* **Styling**: Tailwind CSS
* **Tools**: `uvicorn`, `vite`, `requests`
