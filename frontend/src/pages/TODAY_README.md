# Страница "Сегодня" (TodayPage)

Этот документ описывает логику работы и устройство страницы "Сегодня", которая отображает план питания на текущий день.

## Основной макет

Страница разделена на две основные области:

1.  **Левая панель (Сайдбар)** - Занимает 25% ширины экрана.
    *   **Статистика по семье**: В верхней части отображается статистика калорийности для каждого члена семьи на текущий день.
    *   **Расписание (Лента блюд)**: Список всех запланированных приемов пищи.
        *   Отображает название рецепта, для кого предназначено, калорийность, стоимость и размер порции.
        *   Активный (выбранный) элемент подсвечивается.

2.  **Правая панель (Детали)** - Занимает 75% ширины экрана.
    *   Отображает подробную информацию о выбранном блюде.
    *   Занимает всё доступное пространство для удобства чтения.

## Логика выбора рецепта по умолчанию

При загрузке страницы система автоматически выбирает рецепт для отображения по следующему алгоритму:

1.  **Утреннее время (00:00 - 09:00)**:
    *   Система ищет блюдо с типом "Завтрак" (`breakfast`).
    *   Если найдено, оно выбирается автоматически.
    *   Если завтрака нет, срабатывает стандартная логика.

2.  **Стандартная логика (остальное время или если нет завтрака)**:
    *   Выбирается первое доступное блюдо из списка согласно порядку приемов пищи.

## Особенности интерфейса

### Утренний режим (00:00 - 09:00)
Если пользователь заходит на страницу утром, над деталями выбранного рецепта появляется дополнительный блок:
*   **"🎒 Не забыть взять с собой!"**: Список блюд, помеченных как `takeaway` (Взять с собой).
*   Включает чекбоксы, чтобы пользователь мог отмечать уже собранные ланчбоксы.

### Детали рецепта
*   **Ингредиенты**: Расположены в левой колонке (фиксированная ширина ~320px).
*   **Способ приготовления**: Занимает всё оставшееся пространство справа.
*   **Масштабирование**: Количество ингредиентов и КБЖУ пересчитываются пропорционально количеству порций в плане относительно базового рецепта.

## Технические детали
*   Файл: `TodayPage.jsx`
*   Зависимости: `React`, `TopBar` (глобальный), `fetch` (API).
*   API: Использует `/api/plan/` для получения данных.
